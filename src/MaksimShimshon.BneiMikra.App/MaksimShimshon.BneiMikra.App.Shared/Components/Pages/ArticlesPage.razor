@inherits FluxorComponent
@page "/articles"

<PageTitle>@("B'nei Mikra - Home")</PageTitle>
@if (SearchState.Value.IsLoading)
{
    <MudItem xs="12">
        <MudProgressLinear Color="Color.Primary" Indeterminate="true" Class="my-7" />
    </MudItem>
}else if (SearchState.Value.Result != default)
{
    @if (SearchState.Value.Result.Result.Count <= 0)
    {
        <MudItem xs="12">
            <MudText>
                @AppResourceProvider.GetString(()=> ApplicationResource.HttpStatusNotFoundDescription)
            </MudText>
        </MudItem>
    }
    else
    {
        @foreach (var item in SearchState.Value.Result.Result)
        {
            <MudItem xs="12" sm="6" md="4" lg="3">
                <ArticleLiteItem Data=item />
            </MudItem>
        }
    }
}
else
{
    
}

